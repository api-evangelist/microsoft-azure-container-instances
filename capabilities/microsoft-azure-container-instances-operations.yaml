naftiko: 1.0.0-alpha2
info:
  label: Azure Container Instances REST API — Operations
  description: 'Azure Container Instances REST API — Operations. 1 operations. Lead operation: Azure Container Instances REST
    API List operations. Self-contained Naftiko capability covering one Microsoft Azure Container Instances business surface.'
  tags:
  - Microsoft Azure Container Instances
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_CONTAINER_INSTANCES_API_KEY: MICROSOFT_AZURE_CONTAINER_INSTANCES_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-container-instances-operations
    baseUri: https://management.azure.com
    description: Azure Container Instances REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.ContainerInstance-operations
      path: /providers/Microsoft.ContainerInstance/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Container Instances REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_CONTAINER_INSTANCES_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-container-instances-operations-rest
    port: 8080
    description: REST adapter for Azure Container Instances REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-containerinstance/operations
      name: providers-microsoft-containerinstance-operations
      description: REST surface for providers-Microsoft.ContainerInstance-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Container Instances REST API List operations
        call: microsoft-azure-container-instances-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-container-instances-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Container Instances REST API — Operations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: azure-container-instances-rest-api
      description: Azure Container Instances REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-container-instances-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
